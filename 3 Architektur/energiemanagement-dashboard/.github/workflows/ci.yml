name: Test and Build EnergieDashboard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🔧 Node.js Setup
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📂 In Projektordner wechseln
        run: cd '3 Architektur/energiemanagement-dashboard'

      - name: 📦 Abhängigkeiten installieren
        run: npm install
        working-directory: '3 Architektur/energiemanagement-dashboard'

      - name: 🧪 Tests ausführen
        run: npm test
        working-directory: '3 Architektur/energiemanagement-dashboard'
